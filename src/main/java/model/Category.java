package model;

// Categoria do evento, escolhida pelo usuario no formulario de criacao (RF09).
// Os valores tambem aparecem direto na coluna category do events.csv.
public enum Category {
    MEETING,      // reuniao
    BIRTHDAY,     // aniversario
    APPOINTMENT   // compromisso/consulta
}
